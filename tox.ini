[tox]
envlist=py36

[testenv]
deps = 
    pytest
    pytest-mock
    pytest-cov
    hypothesis
commands = 
    pytest --cov={envsitepackagesdir}/observatory --cov-report=xml tests/ {posargs}
passenv=CC_TEST_REPORTER_ID